name: Deploy

on:
  push:
    tags: ['v*.*.*']

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. Determinar Directorio Destino según el Tag
      - name: Determine Target Directory
        id: target
        run: |
          TAG="${{ github.ref_name }}"
          echo "Analizando tag: $TAG"
          
          echo "TARGET_DIR=${{ secrets.PROJECT_PATH }}" >> $GITHUB_ENV
          echo "BRANCH_NAME=main" >> $GITHUB_ENV
          echo "TAG_NAME=$TAG" >> $GITHUB_ENV

      # 4. Ejecutar comandos SSH (Password)
      - name: Deploy to VM via SSH Password
        run: |
          # Instalar sshpass (necesario para pasar password en modo no interactivo)
          sudo apt-get update
          sudo apt-get install -y sshpass

          # Definir Variables
          TARGET_DIR="${{ secrets.PROJECT_PATH }}"
          BRANCH_NAME="main"
          TAG_NAME="${{ github.ref_name }}"

          echo "🚀 Conectando a ${{ secrets.SSH_IP_SERVER }} usando contraseña..."
          
          # Debug: Verificar longitud de variables (sin revelar contenido)
          echo "Longitud User: $(echo -n "${{ secrets.SSH_USER_NAME }}" | wc -c)"
          echo "Longitud Pass: $(echo -n "${{ secrets.SSH_PRIVATE_KEY }}" | wc -c)"
          echo "Longitud Host: $(echo -n "${{ secrets.SSH_IP_SERVER }}" | wc -c)"
          echo "Puerto: ${{ secrets.SSH_PORT }}"

          # Ejecutar SSH usando sshpass con verbose (-v) para ver qué pasa en el handshake
          # Y exportando la variable SSHPASS para evitar problemas con caracteres especiales en línea de comandos
          export SSHPASS="${{ secrets.SSH_PRIVATE_KEY }}"
          sshpass -e ssh -v -o ConnectTimeout=60 -o ServerAliveInterval=60 -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER_NAME }}@${{ secrets.SSH_IP_SERVER }} \
            "TARGET_DIR='$TARGET_DIR' BRANCH_NAME='$BRANCH_NAME' TAG_NAME='$TAG_NAME' bash -s" << 'EOF'
            set -e

            # Validar variables
            if [ -z "$TARGET_DIR" ]; then
              echo "❌ Error: TARGET_DIR no definido."
              exit 1
            fi

            # Entrar al directorio
            git config --global --add safe.directory \"$TARGET_DIR\"
            cd "$TARGET_DIR"

            echo "🚀 Usuario autenticado: $(whoami)"
            echo "📦 Actualizando código con tag: $TAG_NAME"
            echo "🌿 Rama a usar: $BRANCH_NAME"
            
            # Git Checkout
            git checkout "$BRANCH_NAME"
            git fetch --prune --prune-tags --force
            git fetch origin tag "$TAG_NAME"
            git checkout --force "$TAG_NAME"
            
            # Dependencias Backend
            echo "🔧 Composer Install..."
            php composer.phar install --no-interaction --prefer-dist --optimize-autoloader --no-dev
            
            # Migrar Datos
            echo "🗄️ Migrando base de datos..."
            php artisan migrate --force
            
            # Limpieza de caché
            echo "🧹 Limpieza de caché..."
            php artisan optimize
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            
            if php artisan list | grep -q 'filament:optimize'; then
              php artisan filament:optimize
            fi

            echo "✅ Despliegue completado!"
          EOF
